# --- H2 ???? DB (MySQL ?? ??) ---
spring.datasource.url=jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# SQL ??? ??(??? ??)
spring.sql.init.mode=never

# --- ?? ?? ? ---
spring.mail.host=localhost
spring.mail.port=2525
spring.mail.username=dummy
spring.mail.password=dummy
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# --- ? ??? ???? ?? ? ---
app.auth.temp-password-expiration-minutes=10
app.auth.temp-password-length=8

# JWT/?? ??(????? ?? ??)
jwt.secret=TEST_SECRET_0123456789
jwt.expiration-minutes=60

# ?? ??? ?????? ???(??)
server.port=0

org.gradle.java.home=C:\\Program Files\\Java\\jdk-17.0.2
